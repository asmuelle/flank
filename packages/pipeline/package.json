{
  "name": "@flank/pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Pipeline logic: source adapters, hash-gated ingest, mock triage, in-memory store, fixture scenario. Pure functions over content strings; I/O stays at the edges.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./store-contract": "./src/store-contract.ts",
    "./inngest": "./src/inngest.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "@flank/core": "workspace:*",
    "cron-parser": "^5.0.4",
    "inngest": "^4.7.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
