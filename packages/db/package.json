{
  "name": "@flank/db",
  "version": "0.1.0",
  "private": true,
  "description": "Drizzle ORM schema for Postgres + pgvector. Schema-only in M1: tests never need a live database.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@flank/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@flank/pipeline": "workspace:*",
    "@types/node": "^26.0.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
